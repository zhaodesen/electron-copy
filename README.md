### 本地运行
```shell
npm run dev

```
### 本地运行(c#有修改)
```shell
npm run dev:with-publish
```

### 本地打包
```shell
npm run dist:local
```

### 本地打包并上传github
```shell
npm run dist
```

### git 打 Tag 推送会自动发布

### git push 会自动构建
